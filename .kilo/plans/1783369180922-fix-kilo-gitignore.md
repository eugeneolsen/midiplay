# Plan: Fix .gitignore for .kilo/ directory tracking

## Summary
Replace the blanket `.kilo/` ignore and `!kilo.jsonc` negation with selective ignores, letting `.kilo/` be tracked by git. The inner `.kilo/.gitignore` already handles dependencies (node_modules, lock files, etc.), so the root gitignore only needs to cover runtime artifacts.

## Current state (lines 855-877)
```
# Kilo Code Configuration
!.kilo/
.kilo/*
!.kilo/kilo.jsonc
kilo/node_modules/
node_modules/
kilo/*.log
kilo/.local/
.kilocode/snapshots/
.kilocode/index/
lancedb/
qdrant/
```

## Final state (lines 855-872)
```
# =========================================================================
# Kilo Code Configuration
# =========================================================================

# IGNORE: Local session persistence, cached files, and logs
.kilo/*.log
.kilo/.local/

# IGNORE: Agent Manager worktrees (generated, large)
.kilo/worktrees/

# IGNORE: Project-level codebase indices (prevents bloated commits)
.kilocode/snapshots/
.kilocode/index/
lancedb/
qdrant/
```

## What's tracked in .kilo/ (no root gitignore blocks them)
- `.kilo/kilo.jsonc` — config file (inner .gitignore doesn't exclude it)
- `.kilo/command/*.md` — custom slash commands
- `.kilo/agent/*.md` — custom agents
- `.kilo/skills/*/SKILL.md` — custom skills
- `.kilo/themes/*.json` — custom themes
- `.kilo/setup-script`, `.kilo/run-script` — scripts

## What's ignored

### By root .gitignore (new patterns):
- `.kilo/*.log` — log files
- `.kilo/.local/` — session persistence
- `.kilo/worktrees/` — Agent Manager checkouts
- `.kilocode/snapshots/`, `.kilocode/index/`, `lancedb/`, `qdrant/` — indices

**Design decision:** `.kilo/*.log` and `.kilo/.local/` stay in root `.gitignore` rather than moving to `.kilo/.gitignore` because the inner `.gitignore` appears to be Kilo-managed and may be regenerated by tooling upgrades, which would wipe custom entries.

### By inner `.kilo/.gitignore` (existing, no changes needed):
- node_modules, package.json, package-lock.json, pnpm-lock.yaml, bun.lock, yarn.lock
- .gitignore, agent-manager.json

## Tasks

1. Replace lines 855-877 with the final state block above
2. Verify with `git check-ignore -v`:
   - `.kilo/kilo.jsonc` → NOT ignored (tracked)
   - `.kilo/node_modules` → ignored (by inner .gitignore)
   - `.kilo/.local/` → ignored (by root .gitignore)
   - `.kilo/worktrees/` → ignored (by root .gitignore)

## Validation
```bash
git check-ignore -v .kilo/kilo.jsonc      # should show NOT ignored or negated
git check-ignore -v .kilo/node_modules    # should be ignored
git check-ignore -v .kilo/.local/         # should be ignored
git check-ignore -v .kilo/worktrees/      # should be ignored
```